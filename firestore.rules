rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // WARNING: These rules are permissive and are not recommended for production.
    // They are configured to allow for rapid development and should be secured
    // before deploying to a live environment.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
